var config = {
    host : 'localhost',
    user : 'root',
    password : 'root',
    // en blanco para las otras computadoras que no sean la de ahiezer
    // password:'',
    database : 'proyecto1'
};

module.exports = config;

